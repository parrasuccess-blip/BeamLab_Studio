{
  "name": "beamlab-studio",
  "private": true,
  "version": "4.1.3",
  "description": "BeamLab Studio - deterministic structural analysis, learning and transparent design review.",
  "scripts": {
    "build": "node scripts/build.cjs",
    "test:browser": "playwright test",
    "test": "node scripts/build.cjs && node --test tests/*.test.cjs tests/*.test.mjs tests/legacy/*.test.cjs"
  },
  "engines": {
    "node": ">=24"
  },
  "type": "module",
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
